# NOWEX UI – مستند‌سازی مراحل انجام‌شده

## 📌 مقدمه

این پروژه شامل دو بخش اصلی است:

- **user-ui**: رابط کاربری کاربران
- **admin-ui**: داشبورد مدیریت

هدف ما ایجاد یک معماری ماژولار و آینده‌نگر برای توسعه‌ی پایدار و سریع است.

---

## ✅ مراحل انجام‌شده

### 1. آماده‌سازی محیط توسعه

- ایجاد پروژه‌ی `user-ui` بر اساس Next.js 15 و App Router.
- راه‌اندازی TailwindCSS و رفع خطاهای PostCSS با نصب پکیج جدید `@tailwindcss/postcss`.
- تنظیم فایل‌های کانفیگ:
  - `tailwind.config.js` برای مسیرهای `app` و `components`.
  - `postcss.config.js` با پلاگین‌های `@tailwindcss/postcss` و `autoprefixer`.
- ایمپورت استایل‌ها در `app/layout.tsx` با `globals.css`.

---

### 2. ساختار پوشه‌ها

ایجاد پوشه‌ی **`lib/services`** برای لایه‌ی داده (Data Service Layer):

user-ui/
app/
dashboard/
page.tsx
layout.tsx
globals.css
lib/
services/
apiClient.ts
authService.ts
userService.ts

Code

---

### 3. سرویس‌ها

- **apiClient.ts**  
  مدیریت درخواست‌ها با Axios و interceptor برای توکن و خطاها.
- **authService.ts**  
  سرویس ورود و خروج کاربر با Zod برای validation پاسخ.
- **userService.ts**  
  سرویس گرفتن پروفایل کاربر با Zod schema.

---

### 4. وابستگی‌ها

- نصب **Axios** برای ارتباط با API.
- نصب **Zod** برای اعتبارسنجی داده‌ها.
- رفع خطاهای مربوط به cache با پاک کردن پوشه‌ی `.next`.

---

### 5. صفحات تستی

- ایجاد صفحه‌ی **`app/dashboard/page.tsx`** برای نمایش پروفایل کاربر.
- استفاده از `getUserProfile` برای گرفتن داده‌ها.
- نمایش داده‌ها در UI با Tailwind (کارت مرکزی، متن خوش‌آمدگویی، ایمیل و شناسه).
- تست اولیه با **Mock Data** (کاربر Mehdi با ایمیل تستی).

---

### 6. نتایج تست

اجرای موفق پروژه با `npm run dev` و نمایش داشبورد کاربر با داده‌ی تستی:

داشبورد کاربر
خوش آمدید، Mehdi
ایمیل: mehdi@example.com
شناسه: 123

Code

---

### 7. زیرساخت آماده

- Data Service Layer ماژولار و قابل گسترش.
- مدیریت توکن در interceptor.
- Validation با Zod.
- آماده برای توسعه‌ی بخش‌های بعدی (داشبورد ادمین، سیستم کشینگ، سرویس‌های معاملات و گزارش‌ها).

---

## 🔮 گام‌های بعدی

- ساخت صفحه‌ی **Login** در `user-ui` برای جریان ورود → داشبورد.
- شروع توسعه‌ی **داشبورد ادمین** با layout پایه (sidebar, header, content).
- اضافه کردن **سیستم کشینگ** در آینده (React Query یا Axios cache adapter).

---
