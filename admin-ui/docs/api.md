# مستندات API

## مقدمه

این فایل توضیحات مربوط به سرویس‌ها و endpointهای بک‌اند پروژه NOWEX را ارائه می‌دهد. هدف این است که تیم توسعه بتواند ورودی‌ها و خروجی‌های هر API را بررسی و تست کند.

---

## Endpointها

### GET /api/dashboard

- **ورودی:**
  - `start`: تاریخ شروع بازه (YYYY/MM/DD)
  - `end`: تاریخ پایان بازه (YYYY/MM/DD)
- **خروجی:**
  ```json
  {
    "trades": [...],
    "users": {...},
    "fees": {...}
  }
  POST /api/trade
  ورودی:
  ```

json
{
"userId": "string",
"amount": 100000,
"type": "buy|sell"
}
خروجی:

json
{
"status": "success",
"tradeId": "string"
}
GET /api/users
ورودی: بدون پارامتر

خروجی: لیست کاربران (کل و فعال)

نکات
همه درخواست‌ها باید با توکن احراز هویت ارسال شوند.

پاسخ‌ها در قالب JSON هستند.

Code

---

### 📂 فایل بعدی: `architecture.md`

```markdown
# معماری سیستم

## جریان داده‌ها

UI → Service Layer → Compute Engine → Backend

---

## توضیحات اجزا

- **UI (Next.js + React):** نمایش داشبورد و کارت‌ها، دریافت ورودی کاربر.
- **Service Layer:** ارتباط با API بک‌اند، مدیریت درخواست‌ها و پاسخ‌ها.
- **Compute Engine:** پردازش داده‌های خام، محاسبه سود، ضرر، کارمزد و بالانس.
- **Backend:** پایگاه داده و API برای ذخیره و ارائه داده‌ها.

---

## دیاگرام ساده

[User Interface] → [Service Layer] → [Compute Engine] → [Backend/API → Database]
```
